% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_causal_scm.r
\name{nm_scm_all}
\alias{nm_scm_all}
\title{Run Synthetic Control for Many Treated Units (Batch Processing)}
\usage{
nm_scm_all(
  df,
  date_col,
  outcome_col,
  unit_col,
  donors = NULL,
  cutoff_date,
  scm_backend = "scm",
  model_config = NULL,
  n_cores = NULL,
  max_mem_size = NULL,
  verbose = TRUE,
  shutdown_on_exit = NULL,
  ...
)
}
\arguments{
\item{df}{A long-format panel data frame.}

\item{date_col}{The name of the date column.}

\item{outcome_col}{The name of the outcome variable column.}

\item{unit_col}{The name of the column containing unit identifiers.}

\item{donors}{Optional character vector specifying the global donor pool.
If NULL, all other units are used as donors for each iteration.}

\item{cutoff_date}{The treatment cutoff date in "YYYY-MM-DD" format.}

\item{scm_backend}{The synthetic control method to use: "scm" or "mlscm".}

\item{model_config}{List of model parameters passed to the backend (e.g. list(nfold=5)).}

\item{n_cores}{Number of CPU cores.}

\item{max_mem_size}{Maximum memory for H2O (only used if backend="mlscm").}

\item{verbose}{Logical; whether to print INFO/WARN log messages.}

\item{shutdown_on_exit}{Logical; if TRUE, shuts down H2O after completion.
If NULL (default), it auto-detects: TRUE if we started the cluster, FALSE if we reused one.}

\item{...}{Additional arguments passed to \code{nm_run_scm}.}
}
\value{
A single, long-format data frame containing the combined results for all units.
}
\description{
Iterates through all units in the panel, treating each one as the "treated unit"
in turn (using the others as donors), and runs the Synthetic Control Method.
}
\details{
\strong{H2O Lifecycle Management (for ML-SCM):}
This function employs a "Smart Lifecycle":
\enumerate{
\item If an H2O cluster is already active, it reuses it and leaves it running after completion.
\item If no cluster is active, it starts a new one and shuts it down after completion.
\item You can override this behavior using the \code{shutdown_on_exit} parameter.
}
}
