% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_io_eea.r
\name{nm_fetch_eea_data}
\alias{nm_fetch_eea_data}
\title{Download Air-Quality Data from the EEA}
\usage{
nm_fetch_eea_data(
  country,
  pollutant,
  year_from,
  year_to,
  station = NULL,
  source = "All",
  output = "TEXT",
  keep_columns = NULL
)
}
\arguments{
\item{country}{Character. ISO-2 country code (e.g. \code{"GB"}, \code{"DE"}).}

\item{pollutant}{Character or integer. Pollutant name (\code{"PM2.5"},
\code{"PM10"}, \code{"NO2"}, \code{"O3"}, \code{"SO2"}, \code{"CO"}) or
integer EIONET code.}

\item{year_from, year_to}{Integer. Inclusive year range.}

\item{station}{Character. Specific station code (e.g. \code{"GB0001A"}).
If \code{NULL}, all stations in \code{country} are returned.}

\item{source}{Character. EEA dataflow source. One of \code{"All"},
\code{"E1a"}, \code{"E2a"}. Default \code{"All"}.}

\item{output}{Character. Format requested from the EEA service. Default
\code{"TEXT"}.}

\item{keep_columns}{Character vector. Restrict returned columns to this
subset. Defaults to a standard tidy set.}
}
\value{
\code{data.frame} with at least columns \code{date}, \code{site},
\code{country}, \code{pollutant}, \code{value}, \code{unit},
\code{lat}, \code{lon}. Sorted by \code{(site, date)}.
}
\description{
Queries the EEA Discomap discovery service to obtain a list of CSV URLs for
the requested country/pollutant/year range, downloads each CSV, and
concatenates them into a single tidy \code{data.frame}.
}
