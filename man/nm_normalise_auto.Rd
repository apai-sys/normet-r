% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_norm_normalise.r
\name{nm_normalise_auto}
\alias{nm_normalise_auto}
\title{Auto-Adaptive Weather Normalisation (Auto-Pilot)}
\usage{
nm_normalise_auto(
  df,
  model,
  resample_vars = NULL,
  resample_df = NULL,
  convergence_metric = "series",
  convergence_tol = NULL,
  rse_percentile = 95,
  stability_streak = NULL,
  batch_size = 100,
  max_samples = 5000,
  seed = 7654321,
  verbose = TRUE,
  return_history = FALSE,
  ...
)
}
\arguments{
\item{df}{Input data frame/data.table.}

\item{model}{Trained model object.}

\item{resample_vars}{Variables to resample.}

\item{resample_df}{Data pool to sample from.}

\item{convergence_metric}{\code{"series"} (default) or \code{"global"} (legacy).
See Details.}

\item{convergence_tol}{Numeric fraction or percent string. Stopping
threshold; defaults to \code{"3\%"} for \code{"series"} and \code{"0.5\%"} for
\code{"global"}.}

\item{rse_percentile}{Percentile of the per-date relative-standard-error
distribution tested against \code{convergence_tol} (only used by
\code{"series"}). Default 95.}

\item{stability_streak}{Integer. Consecutive checks required to trigger
stop. Defaults to 3 for \code{"series"} (the RSE declines smoothly as
\code{1/sqrt(n)}) and 5 for \code{"global"} (legacy behaviour).}

\item{batch_size}{Integer. Samples per iteration. Default 100.}

\item{max_samples}{Integer. Hard limit to prevent infinite loops. Default 5000.}

\item{seed}{Integer. Random seed for reproducibility. Each batch uses
\code{seed + cumulative_n} to ensure independent Monte-Carlo resamples.
Default 7654321.}

\item{verbose}{Logical. Print progress.}

\item{return_history}{Logical. If TRUE, also return a per-batch
convergence trace (\code{n}, \code{metric}, \code{global_mean}, \code{stable_count}) --
useful for visualising/auditing the convergence path (e.g. the
"tolerance tunnel" diagnostic plot). Default FALSE.}

\item{...}{Additional arguments forwarded to each batch's \code{\link[=nm_normalise]{nm_normalise()}}
call (e.g. \code{n_cores}, \code{replace}, \code{memory_save},
\code{feature_names}).}
}
\value{
A list containing:
\itemize{
\item \code{best_n}: Total samples used.
\item \code{res}: Data frame with \code{date}, \code{observed}, \code{normalised}.
\item \code{history}: (only if \code{return_history=TRUE}) data frame with one row
per convergence check: \code{n} (cumulative samples), \code{metric} (the value
tested against \code{convergence_tol}), \code{global_mean}, \code{stable_count}.
}
}
\description{
An "Auto-Pilot" wrapper for \code{nm_normalise} that automatically determines the
optimal number of resampling iterations required for convergence.
}
\details{
Instead of guessing \code{n_samples} (e.g., 1000), this function runs
resampling in batches of \code{batch_size} and stops when a convergence
criterion holds for \code{stability_streak} consecutive checks. Two criteria
are available via \code{convergence_metric}:
\itemize{
\item \code{"series"} (default): stop when the \code{rse_percentile}-th percentile of
the \strong{per-date relative standard error} of the cumulative mean falls
below \code{convergence_tol} (default \code{"3\%"}). This targets the actual
deliverable of deweathering -- the per-timestamp series: with the
defaults, at least 95\% of timestamps have a Monte-Carlo standard
error below 3\% of their value when sampling stops. The RSE is a
CLT-based distance-to-limit measure, so the test does not weaken as
the sample count grows.
\item \code{"global"} (legacy): stop when the relative batch-to-batch change of
the \strong{global mean} stays below \code{convergence_tol} (default \code{"0.5\%"}).
Two caveats motivated the new default above: (i) the global mean
averages over all timestamps, suppressing Monte-Carlo noise by roughly
\code{sqrt(n_dates)}, so it can satisfy a tight tolerance while individual
timestamps are still far from converged; and (ii) the increment of a
cumulative mean shrinks like \code{1/n_batches} automatically, so the test
gets weaker as sampling proceeds and cannot catch late, slow drift.
}
}
