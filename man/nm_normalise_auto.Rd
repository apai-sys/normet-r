% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_norm_normalise.r
\name{nm_normalise_auto}
\alias{nm_normalise_auto}
\title{Auto-Adaptive Weather Normalisation (Auto-Pilot)}
\usage{
nm_normalise_auto(
  df,
  model,
  resample_vars = NULL,
  resample_df = NULL,
  convergence_tol = "0.5\%",
  stability_streak = 5,
  batch_size = 100,
  max_samples = 5000,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{df}{Input data frame/data.table.}

\item{model}{Trained model object.}

\item{resample_vars}{Variables to resample.}

\item{resample_df}{Data pool to sample from.}

\item{convergence_tol}{Numeric (0.005) or String ("0.5\%"). Stability threshold.}

\item{stability_streak}{Integer. Consecutive checks required to trigger stop. Default 5.}

\item{batch_size}{Integer. Samples per iteration. Default 100.}

\item{max_samples}{Integer. Hard limit to prevent infinite loops. Default 5000.}

\item{verbose}{Logical. Print progress.}

\item{...}{Additional arguments forwarded to each batch's \code{\link[=nm_normalise]{nm_normalise()}}
call (e.g. \code{n_cores}, \code{seed}, \code{replace}, \code{memory_save},
\code{feature_names}).}
}
\value{
A list containing:
\itemize{
\item \code{best_n}: Total samples used.
\item \code{res}: Data frame with \code{date}, \code{observed}, \code{normalised}.
}
}
\description{
An "Auto-Pilot" wrapper for \code{nm_normalise} that automatically determines the
optimal number of resampling iterations required for convergence.
}
\details{
Instead of guessing \code{n_samples} (e.g., 1000), this function:
\enumerate{
\item Runs simulations in small batches (e.g., 100 samples).
\item Accumulates the daily sums and counts.
\item Monitors the stability of the global mean normalised concentration.
\item Stops when the global mean changes by less than \code{convergence_tol} for \code{stability_streak} checks.
}
}
