% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_causal_bayesian.r
\name{nm_bayesian_scm}
\alias{nm_bayesian_scm}
\title{Bayesian SCM with Simplex Weights}
\usage{
nm_bayesian_scm(
  df,
  date_col = "date",
  unit_col = "code",
  outcome_col = "poll",
  treated_unit = NULL,
  cutoff_date = NULL,
  donors = NULL,
  dirichlet_alpha = 1,
  sigma_prior = 1,
  draws = 2000,
  burnin = 1000,
  ci_level = 0.95,
  seed = 7654321,
  auto_cutoff = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{df}{Long data frame with columns for date, unit, and outcome.}

\item{date_col}{Name of the date column (coercible to Date).}

\item{unit_col}{Name of the unit identifier column.}

\item{outcome_col}{Name of the outcome column.}

\item{treated_unit}{The treated unit identifier.}

\item{cutoff_date}{The intervention cutoff date (character or Date).}

\item{donors}{Optional character vector of donor unit identifiers. If NULL, uses all non-treated units.}

\item{dirichlet_alpha}{Numeric. Symmetric Dirichlet concentration (default 1.0).
Values < 1 favour sparse weights; > 1 favour diffuse weights.}

\item{sigma_prior}{Numeric. Scale for the HalfNormal prior on noise SD (default 1.0).}

\item{draws}{Integer. Posterior samples after burn-in (default 2000).}

\item{burnin}{Integer. MCMC burn-in iterations (default 1000).}

\item{ci_level}{Numeric. Credible interval level (default 0.95).}

\item{seed}{Integer. Random seed (default 7654321).}

\item{auto_cutoff}{Logical. If TRUE and \code{cutoff_date} is NULL, detect
cutoff from anomaly events (default FALSE).}

\item{verbose}{Logical. Print progress (default TRUE).}
}
\value{
A list with \code{synthetic} (data.frame with observed, synthetic,
synthetic_low, synthetic_high, effect, effect_low, effect_high),
\code{weights} (mean posterior weights), and
\code{weights_summary} (data.frame with mean, hdi_low, hdi_high).
}
\description{
Bayesian synthetic control with Dirichlet prior on donor weights and
Normal likelihood. Uses a simple adaptive Metropolis-Hastings sampler
on the simplex (or rstan if available).
}
