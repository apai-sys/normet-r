% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_causal_bands.r
\name{nm_plot_effect_with_bands}
\alias{nm_plot_effect_with_bands}
\title{Plot Treatment Effect with Uncertainty Bands}
\usage{
nm_plot_effect_with_bands(
  bands_df,
  cutoff_date = NULL,
  title = "Effect with Placebo Bands",
  band_label = "placebo band"
)
}
\arguments{
\item{bands_df}{A data frame containing an \code{effect} column and either a
\code{date} or \code{event_time} column. Band columns may be named \code{lower}/\code{upper}
or \code{low}/\code{high}; if neither pair is present, only the effect line is drawn.}

\item{cutoff_date}{Optional. Vertical reference line marking the
intervention. A string/Date when \code{bands_df} has a \code{date} column, or an
integer (e.g. \code{0}) when it has an \code{event_time} column.}

\item{title}{The title for the plot.}

\item{band_label}{Character; legend label for the shaded band (default
\code{"placebo band"}). Used by \code{\link[=nm_plot_uncertainty_bands]{nm_plot_uncertainty_bands()}} to distinguish
"jackknife band" / "bootstrap band".}
}
\value{
A ggplot object.
}
\description{
\code{nm_plot_effect_with_bands} creates a ggplot visualization of a treatment
effect and its corresponding uncertainty bands. Supports output from both
\code{\link[=nm_effect_bands_space]{nm_effect_bands_space()}} (date-indexed) and event-time bands derived from
\code{\link[=nm_effect_bands_time]{nm_effect_bands_time()}} (an \code{event_time} column), provided an \code{effect}
column has been attached.
}
