% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_utils_featureeng.r
\name{nm_add_rolling_features}
\alias{nm_add_rolling_features}
\title{Add Rolling Window Statistics}
\usage{
nm_add_rolling_features(
  df,
  cols,
  windows,
  aggs = "mean",
  min_periods = NULL,
  group_col = NULL,
  date_col = "date",
  suffix = "_roll",
  causal = TRUE
)
}
\arguments{
\item{df}{A data frame with a datetime \code{date} column.}

\item{cols}{Character vector. Columns to compute rolling stats for.}

\item{windows}{Integer vector. Window sizes in rows.}

\item{aggs}{Character vector. Aggregation functions.
One or more of \code{"mean"}, \code{"sd"}, \code{"min"}, \code{"max"},
\code{"median"}, \code{"sum"}.}

\item{min_periods}{Integer. Minimum periods for rolling window (defaults to window size).}

\item{group_col}{Character. Optional column name for within-group rolling stats.}

\item{date_col}{Character. Date column name.}

\item{suffix}{Character. Suffix for new columns. Default \code{"_roll"}.}

\item{causal}{Logical. If TRUE (default), use trailing windows (no look-ahead).
If FALSE, use centered windows.}
}
\value{
A data frame with rolling statistic columns appended.
}
\description{
Compute rolling window statistics (mean, sd, min, max, median, sum) for
specified columns.
}
