% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_backend_h2o.r
\name{nm_train_h2o}
\alias{nm_train_h2o}
\title{Train a model using H2O AutoML}
\usage{
nm_train_h2o(
  df,
  target = "value",
  covariates = NULL,
  model_config = NULL,
  seed = 7654321,
  verbose = TRUE
)
}
\arguments{
\item{df}{Prepared data frame containing the data. If a "set" column exists, filters for "training".}

\item{target}{Name of the target column (response variable).}

\item{covariates}{Character vector of predictor column names.}

\item{model_config}{Optional list of AutoML configuration overrides.
Examples: \code{list(max_runtime_secs = 60, include_algos = c("GBM"), nfolds = 5)}.}

\item{seed}{Random seed for reproducibility. Default is 7654321.}

\item{verbose}{Whether to print progress and diagnostics. Default is TRUE.}
}
\value{
The trained H2O model object (the leader model), with attributes \code{backend="h2o"}.
}
\description{
\code{nm_train_h2o} trains a model using H2O AutoML with built-in retry logic.
}
\details{
This function focuses purely on model training. It assumes the H2O cluster is managed
externally (e.g., via \code{nm_init_h2o}). If the cluster is unstable, it attempts to
restart it using the default settings defined in \code{nm_init_h2o}.

Key steps:
\itemize{
\item \strong{Data Selection}: Filters for "training" set.
\item \strong{Sanitization}: Checks variance and fills missing factor levels.
\item \strong{Self-Healing}: Restarts H2O (with default args) if training crashes.
}
}
