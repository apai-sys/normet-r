% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_norm_decom.r
\name{nm_decom_met}
\alias{nm_decom_met}
\title{Decompose Meteorological Influences (Weather Contributions)}
\usage{
nm_decom_met(
  df = NULL,
  model = NULL,
  value = "value",
  predictors = NULL,
  backend = "lightgbm",
  split_method = "random",
  fraction = 0.75,
  model_config = NULL,
  n_samples = 300,
  seed = 7654321,
  importance_ascending = FALSE,
  n_cores = NULL,
  max_mem_size = NULL,
  resample_df = NULL,
  memory_save = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{df}{The input data frame. Must contain a 'date' column.}

\item{model}{Pre-trained model. If NULL, a model will be trained.}

\item{value}{The target variable name as a string (default 'value').}

\item{predictors}{Character vector of features used for \strong{training} (if model is NULL).}

\item{backend}{The modeling backend (default 'lightgbm', or 'h2o').}

\item{split_method}{Method for splitting data (e.g., 'random').}

\item{fraction}{Proportion of data used for training.}

\item{model_config}{List of configuration parameters for model training.}

\item{n_samples}{Number of resampling iterations per step (default 300).}

\item{seed}{Random seed for reproducibility.}

\item{importance_ascending}{Logical. If TRUE, decomposes variables from least to most important.
If FALSE (default), decomposes from most important to least.}

\item{n_cores}{Number of CPU cores to use for \strong{H2O Initialization} (when
\code{backend = "h2o"}) and for parallel resampling in each
\code{nm_normalise()} call (default: detected cores minus one).}

\item{max_mem_size}{Maximum memory for H2O (e.g., "16G"). If NULL, auto-detected.}

\item{resample_df}{External resampling pool. \strong{Important}: Usually the full dataset.
If NULL, defaults to \code{df}.}

\item{memory_save}{Logical. Enable memory-efficient processing.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
A data frame containing:
\itemize{
\item \code{observed}: The original time series.
\item \code{emi_total}: The weather-normalised trend.
\item \code{met_total}: The total meteorological component (\code{observed} - \code{emi_total}).
\item \code{met_base}: The average meteorological influence (constant).
\item \code{met_noise}: Unexplained meteorological variance.
\item Individual columns for each weather variable (e.g., \code{ws}, \code{temp}, \code{wd}).
}
}
\description{
\code{nm_decom_met} quantifies the specific contribution of individual meteorological variables
to the target variable (e.g., "How much did Wind Speed contribute vs Temperature?").
}
\details{
The function uses a sequential "Freeze-and-Shuffle" approach:
\enumerate{
\item \strong{Step 1 (emi_total)}: Calculate the trend where \strong{ALL} weather variables are shuffled (resampled). This removes all weather influence.
\item \strong{Step 2 (First Weather Var)}: Freeze the first variable (use observed values) while keeping others shuffled. The difference from Step 1 is the contribution of this variable.
\item \strong{Step 3 (Next Weather Var)}: Freeze the next variable (plus previous ones) and compare to the previous state.
\item \strong{Residuals}: \code{met_noise} captures the variance not explained by the model's main effects.
}
}
