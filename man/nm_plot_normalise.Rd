% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_plotting.r
\name{nm_plot_normalise}
\alias{nm_plot_normalise}
\title{Normalisation Result Plot}
\usage{
nm_plot_normalise(
  result_df,
  observed_col = "observed",
  normalised_col = "normalised",
  ci_low = NULL,
  ci_high = NULL,
  resample = NULL,
  title = NULL,
  ylabel = "Concentration",
  ...
)
}
\arguments{
\item{result_df}{Data frame with \code{date}, \code{observed},
\code{normalised} columns, and optionally quantile columns.}

\item{observed_col}{Character. Observed column. Default "observed".}

\item{normalised_col}{Character. Normalised column. Default "normalised".}

\item{ci_low}{Character. Lower bound column (e.g. "q025").}

\item{ci_high}{Character. Upper bound column (e.g. "q975").}

\item{resample}{Character. Pandas-style resample rule (e.g. \code{"D"} for
daily, \code{"W"} for weekly) or a
\code{\link[lubridate:floor_date]{lubridate::floor_date}} unit
(e.g. \code{"day"}, \code{"week"}). If given, the series are
averaged to this frequency before plotting. Default \code{NULL}
(no resampling).}

\item{title}{Character. Optional title.}

\item{ylabel}{Character. Y-axis label. Default "Concentration".}

\item{...}{Additional arguments passed to ggplot2.}
}
\value{
A ggplot2 object.
}
\description{
Plot observed vs. normalised (deweathered) time series with optional
uncertainty band.
}
\examples{
\donttest{
if (requireNamespace("lightgbm", quietly = TRUE)) {
  covariates <- c("u10", "v10", "d2m", "t2m", "blh", "ssrd")
  predictors <- c(covariates, "date_unix", "day_julian", "weekday", "hour")
  build <- nm_build_model(
    my1[1:150, c("date", "NO2", covariates)],
    value = "NO2", predictors = predictors,
    model_config = list(n_trials = 1, cv_folds = 2, nrounds = 15,
                         num_leaves_min = 5, num_leaves_max = 15),
    seed = 42, verbose = FALSE
  )
  norm <- nm_normalise(
    build$df_prep, build$model,
    resample_vars = covariates, n_samples = 5, n_cores = 1, verbose = FALSE
  )
  nm_plot_normalise(norm)
}
}

}
