% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_causal_bands.r
\name{nm_effect_bands_space}
\alias{nm_effect_bands_space}
\title{Build Uncertainty Bands from Placebo-in-Space Results}
\usage{
nm_effect_bands_space(
  placebo_space_out,
  level = 0.95,
  method = "quantile",
  verbose = TRUE
)
}
\arguments{
\item{placebo_space_out}{The list returned by \code{nm_placebo_in_space}.}

\item{level}{Numeric; the confidence level for the bands (default 0.95).}

\item{method}{Character; method for band construction: "quantile" (default) or "std".}

\item{verbose}{Logical; should the function print log messages? Default is TRUE.}
}
\value{
A data frame containing:
\itemize{
\item \code{date}: Timestamp.
\item \code{effect}: The observed effect (Treatment - Synthetic).
\item \code{lower}: The lower bound of the placebo distribution.
\item \code{upper}: The upper bound of the placebo distribution.
\item \code{plc_q_low}, \code{plc_q_high}: (method = "quantile") raw placebo
quantiles at \code{alpha}/\code{1 - alpha} (identical to \code{lower}/\code{upper}).
\item \code{plc_p10}, \code{plc_p90}: (method = "quantile") 10th/90th percentiles
of the placebo distribution at each date.
\item \code{plc_mean}, \code{plc_std}, \code{z}: (method = "std") placebo mean,
standard deviation, and the normal critical value used for the bands.
}
}
\description{
\code{nm_effect_bands_space} constructs the uncertainty or significance bands
derived from the distribution of placebo effects (donor units treated as placebos).
}
\details{
This function calculates the range of effects observed in the donor pool.
\itemize{
\item \strong{Method "quantile"}: Calculates the empirical quantiles (e.g., 2.5\% and 97.5\%) of the placebo effects at each time point.
\item \strong{Method "std"}: Calculates the mean +/- z * SD of the placebo effects.
}
Typically, if the observed effect for the treated unit falls outside these bands
during the post-treatment period, the effect is considered statistically significant.
}
