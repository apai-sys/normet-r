% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_model_predict.r
\name{nm_predict}
\alias{nm_predict}
\title{Predict using a trained model}
\usage{
nm_predict(model, newdata, verbose = FALSE, ...)
}
\arguments{
\item{model}{The trained model object.}

\item{newdata}{A data frame or data.table containing the new data for prediction.}

\item{verbose}{Logical. Should the function print log messages? Default is FALSE.}

\item{...}{Additional arguments to be passed to the backend-specific function.}
}
\value{
A numeric vector of predicted values.
}
\description{
\code{nm_predict} is a high-level wrapper that generates predictions from a trained model.
It automatically identifies the model's backend (e.g., H2O) and dispatches to the
appropriate prediction function.
}
\examples{
\donttest{
if (requireNamespace("lightgbm", quietly = TRUE)) {
  covariates <- c("ws", "wd", "temp", "RH", "blh", "ssrd")
  predictors <- c(covariates, "date_unix", "day_julian", "weekday", "hour")
  build <- nm_build_model(
    my1[1:150, c("date", "NO2", covariates)],
    value = "NO2", predictors = predictors,
    model_config = list(n_trials = 1, cv_folds = 2, nrounds = 15,
                         num_leaves_min = 5, num_leaves_max = 15),
    seed = 42, verbose = FALSE
  )
  preds <- nm_predict(build$model, build$df_prep)
  head(preds)
}
}

}
