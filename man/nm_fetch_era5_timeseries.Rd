% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_io_era5.r
\name{nm_fetch_era5_timeseries}
\alias{nm_fetch_era5_timeseries}
\title{Direct point ERA5 timeseries fetching via CDS (CSV Endpoint)}
\usage{
nm_fetch_era5_timeseries(
  sites,
  date_from,
  date_to,
  variables = NULL,
  cache_dir = NULL,
  site_col = "site",
  lat_col = "lat",
  lon_col = "lon",
  date_col = "date",
  user = "ecmwfr"
)
}
\arguments{
\item{sites}{Data.frame or Named list/vector. Coordinates of sites.}

\item{date_from}{Character or Date. Start date.}

\item{date_to}{Character or Date. End date.}

\item{variables}{Character vector. CDS API variable names. Defaults to \code{nm_era5_aq_variables_default}.}

\item{cache_dir}{Character. Directory to cache point CSV files. If provided, reuses existing CSVs.}

\item{site_col}{Character. Site identifier column name. Default \code{"site"}.}

\item{lat_col}{Character. Latitude column name. Default \code{"lat"}.}

\item{lon_col}{Character. Longitude column name. Default \code{"lon"}.}

\item{date_col}{Character. Output date column name. Default \code{"date"}.}

\item{user}{Character. ECMWF/CDS keyring profile name set via
\code{\link[ecmwfr]{wf_set_key}} (default \code{"ecmwfr"}). Use a
different profile if you have separate Atmosphere Data Store (ADS) and
Climate Data Store (CDS) credentials stored — ADS does not serve ERA5.}
}
\value{
A data.frame with columns \code{[site, date, lat, lon, <variables...>]}.
}
\description{
Directly query the Copernicus CDS API for single-point meteorological timeseries,
bypassing gridded NetCDF dependencies by downloading pre-interpolated CSVs directly.
}
\details{
Uses the \code{reanalysis-era5-single-levels-timeseries} dataset on the
Climate Data Store (\url{https://cds.climate.copernicus.eu}). The request
schema for this dataset differs from the classic gridded ERA5 request: the
point location is a \code{location} object (not top-level
\code{latitude}/\code{longitude}), the date range is a single
\code{"from/to"} string, and the output format key is \code{data_format}
(not \code{format}). The CDS API wraps the result CSV in a zip archive;
this function extracts it transparently. The response already uses short
variable names (\code{t2m}, \code{u10}, \code{v10}, ...) — no renaming
needed.
}
