% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_analysis_events.r
\name{nm_detect_events}
\alias{nm_detect_events}
\title{Detect Consecutive Anomalous Intervals}
\usage{
nm_detect_events(
  series,
  value_col = NULL,
  method = "iqr",
  k = 3,
  min_length = 1,
  stl_period = NULL,
  seed = 7654321
)
}
\arguments{
\item{series}{Numeric vector or data frame. If a vector with
\code{names()} that parse as dates (e.g. \code{as.character(Date)},
as produced when subsetting a panel matrix column), or a data frame
with a \code{date} column / date-like row names, \code{start}/\code{end}
in the result will be \code{Date} values; otherwise they will be
1-based integer positions.}

\item{value_col}{Character. Column name if \code{series} is a data frame.}

\item{method}{Character. Anomaly detection method.}

\item{k}{Numeric. IQR multiplier threshold (default 3.0). For isolation
method, the threshold is always 0.}

\item{min_length}{Integer. Minimum number of consecutive flagged timestamps
for an event. Default 1.}

\item{stl_period}{Integer. Period for STL decomposition.}

\item{seed}{Integer. Random seed forwarded to \code{\link{nm_anomaly_scores}}.}
}
\value{
A data frame with columns: \code{start}, \code{end}, \code{n},
\code{max_score}, \code{mean_score}, sorted by \code{max_score}
descending. \code{start}/\code{end} are \code{Date} values when
date information can be recovered from \code{series} (see above),
otherwise 1-based integer positions.
}
\description{
Identify consecutive time intervals where anomaly scores exceed a threshold.
}
