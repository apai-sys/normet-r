% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_io_defra.r
\name{nm_fetch_aurn_measurements}
\alias{nm_fetch_aurn_measurements}
\title{Fetch AURN Hourly Measurements}
\usage{
nm_fetch_aurn_measurements(
  station = NULL,
  pollutant = "PM2.5",
  date_from,
  date_to,
  station_label = NULL
)
}
\arguments{
\item{station}{Integer, character, or vector. Station ID(s) or label
substring(s). If \code{NULL}, fetches all stations measuring the pollutant
(potentially large).}

\item{pollutant}{Character. Pollutant name. Default \code{"PM2.5"}.}

\item{date_from, date_to}{Character or Date. Inclusive UTC date range.}

\item{station_label}{Character. Optional label substring filter (case-insensitive).
Overrides \code{station} when given.}
}
\value{
\code{data.frame} with columns \code{date} (UTC POSIXct), \code{site},
\code{station_id}, \code{pollutant}, \code{value}, \code{unit},
\code{lat}, \code{lon}. Sorted by \code{(site, date)}.
}
\description{
Downloads hourly measurements from the DEFRA UK-AIR SOS API and returns
a long-format \code{data.frame} compatible with \code{normet} pipelines.
}
