% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_causal_bands.r
\name{nm_uncertainty_bands}
\alias{nm_uncertainty_bands}
\title{Build Uncertainty Bands from Bootstrap or Jackknife Results}
\usage{
nm_uncertainty_bands(
  df,
  date_col,
  unit_col,
  outcome_col,
  treated_unit,
  cutoff_date,
  donors = NULL,
  scm_backend = "scm",
  method = "jackknife",
  B = 200,
  seed = 7654321,
  donor_frac = 0.8,
  time_block_days = NULL,
  ci_level = 0.95,
  verbose = TRUE,
  n_cores = NULL,
  max_mem_size = NULL,
  shutdown_on_exit = TRUE,
  ...
)
}
\arguments{
\item{df}{A long-format panel data frame containing the data.}

\item{date_col}{The name of the date column.}

\item{unit_col}{The name of the unit identifier column.}

\item{outcome_col}{The name of the outcome variable column.}

\item{treated_unit}{The identifier of the treated unit.}

\item{cutoff_date}{The intervention date (in "YYYY-MM-DD" format or Date object).}

\item{donors}{Optional character vector of donor units. If NULL, all units other than
the treated unit are used.}

\item{scm_backend}{The synthetic control backend to use: \code{"scm"} (Classic) or
\code{"mlscm"} (Machine Learning/H2O).}

\item{method}{The uncertainty estimation method: \code{"bootstrap"} or \code{"jackknife"}.}

\item{B}{Integer; the number of bootstrap replications (only used if \code{method = "bootstrap"}).
Default is 200.}

\item{seed}{Integer; random seed for reproducibility (only used if \code{method = "bootstrap"}).}

\item{donor_frac}{Numeric; the fraction of donors to sample in each bootstrap replication.
Default is 0.8.}

\item{time_block_days}{Integer; the size of time blocks (in days) for block bootstrap resampling.
If NULL, simple random sampling is used.}

\item{ci_level}{Numeric; the confidence interval level (e.g., 0.95 for 95 percent bands).}

\item{verbose}{Logical; should the function print INFO/WARN log messages? Default is \code{TRUE}.}

\item{n_cores}{Integer; number of CPU cores to use for the H2O cluster (only for \code{mlscm}).
If NULL, defaults to auto-detection.}

\item{max_mem_size}{Character; maximum memory allocation for H2O (e.g., "4g", "16g").
Strongly recommended for \code{mlscm} to prevent crashes.}

\item{shutdown_on_exit}{Logical; if TRUE, shuts down the H2O cluster when the function finishes.
Set to FALSE if you plan to run multiple uncertainty analyses sequentially to save startup time.
Default is TRUE.}

\item{...}{Additional arguments passed to the underlying SCM runner (\code{nm_run_scm}).}
}
\value{
A list containing:
\describe{
\item{treated}{A data frame with the original observed and synthetic paths for the treated unit.}
\item{low}{A named numeric vector representing the lower bound of the uncertainty band.}
\item{high}{A named numeric vector representing the upper bound of the uncertainty band.}
\item{jackknife_effects}{(Optional) If \code{method = "jackknife"}, a data frame containing the effect paths from each jackknife run.}
}
}
\description{
Construct confidence bands for the treatment effect using either
nonparametric bootstrap or leave-one-donor-out jackknife methods.
}
\details{
\strong{Robustness & Resource Management (Auto-Recovery):}
This function implements a defensive "Auto-Recovery" strategy for the \code{mlscm} (H2O) backend.
It defines an internal helper \code{ensure_h2o_alive()} that runs before the global initialization,
before the reference run, and before \emph{every} iteration of the bootstrap/jackknife loops.

If the H2O cluster is found to be inactive, crashed, or unreachable (e.g., due to
"Empty reply from server" errors), the function automatically attempts to restart the
cluster using the provided \code{n_cores} and \code{max_mem_size}. This prevents long-running
jobs from failing completely due to intermittent connection drops.
}
