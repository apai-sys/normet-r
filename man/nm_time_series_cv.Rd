% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_utils_cv.r
\name{nm_time_series_cv}
\alias{nm_time_series_cv}
\title{Walk-Forward Time Series Cross-Validation Splits}
\usage{
nm_time_series_cv(
  df,
  n_splits = 5,
  gap = 0,
  test_size = NULL,
  max_train_size = NULL,
  date_col = "date"
)
}
\arguments{
\item{df}{A data frame with a datetime \code{date_col} column.}

\item{n_splits}{Integer. Number of folds. Default 5.}

\item{gap}{Integer. Rows to skip between train and test. Default 0.}

\item{test_size}{Integer. Test segment size in rows.
If NULL (default), splits the tail evenly into \code{n_splits}.}

\item{max_train_size}{Integer. If given, train is a sliding window of this
many rows; otherwise train is expanding (anchored at start).}

\item{date_col}{Character. Date column name. Default \code{"date"}.}
}
\value{
A list of lists, each with elements \code{train} and \code{test}
containing integer index vectors into the date-sorted data frame.
}
\description{
Generate train/test index pairs for walk-forward (forward-chaining)
cross-validation that respects temporal ordering.
}
