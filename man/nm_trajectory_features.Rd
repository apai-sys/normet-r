% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_io_trajectory.r
\name{nm_trajectory_features}
\alias{nm_trajectory_features}
\title{Reduce one back-trajectory to a feature vector}
\usage{
nm_trajectory_features(traj, source_regions = NULL, prefix = "traj_")
}
\arguments{
\item{traj}{A single trajectory (e.g. one \code{traj} group from
\code{\link{nm_read_trajectory_tdump}}). Needs \code{age_h}, \code{lat}, \code{lon},
\code{height}; uses \code{rainfall} / \code{blh} if present.}

\item{source_regions}{Optional named list of bounding boxes
\code{list(name = c(lon_min, lat_min, lon_max, lat_max))}. For each, the
fraction of trajectory time spent inside is returned as
\verb{<prefix>resid_<name>} (a residence-time proxy).}

\item{prefix}{Character. Prefix for every feature name. Default \code{"traj_"}.}
}
\value{
A named list of transport descriptors: straight-line reach, path
length, mean transport speed, inflow bearing, mean/min height, optional
rainfall sum / mean boundary-layer height, and per-region residence
fractions.
}
\description{
Collapse a single back-trajectory into a fixed-length named list of
transport descriptors.
}
