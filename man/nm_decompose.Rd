% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_norm_decom.r
\name{nm_decompose}
\alias{nm_decompose}
\title{Decompose Time Series Influences}
\usage{
nm_decompose(
  method = "emission",
  df = NULL,
  model = NULL,
  target = "value",
  covariates = NULL,
  backend = "lightgbm",
  split_method = "random",
  train_fraction = 0.75,
  model_config = NULL,
  n_samples = 300,
  seed = 7654321,
  importance_ascending = FALSE,
  n_cores = NULL,
  max_mem_size = NULL,
  resample_df = NULL,
  memory_save = FALSE,
  verbose = TRUE,
  cache_dir = NULL,
  variable_order = NULL
)
}
\arguments{
\item{method}{The decomposition method to use. One of \code{"emission"} or \code{"meteorology"}.}

\item{df}{Data frame containing the input data.}

\item{model}{Optional pre-trained model. If \code{NULL}, a model will be trained.}

\item{target}{The target variable name as a string.}

\item{covariates}{The names of the features used for training (if model is NULL).}

\item{backend}{The modeling backend to use (default 'lightgbm', or 'h2o').}

\item{split_method}{Method for splitting data for model training (e.g.,
'random'). See \code{\link{nm_split_into_sets}} for the exact mechanics
and its warning about \code{"month_ts"}/\code{"season_ts"}'s fixed-position
training blind spot.}

\item{train_fraction}{Proportion of data for training if a model is trained.}

\item{model_config}{A list of configuration parameters for model training.}

\item{n_samples}{Number of samples for the normalisation process.}

\item{seed}{A random seed for reproducibility.}

\item{importance_ascending}{Logical. If \code{TRUE}, sorts meteorological features by
ascending importance. (Used only when \code{method = "meteorology"}).}

\item{n_cores}{Number of CPU cores to use for \strong{H2O Initialization} (when
\code{backend = "h2o"}) and for parallel resampling in each
\code{nm_normalise()} call (default: detected cores minus one).}

\item{max_mem_size}{Maximum memory for H2O (e.g., "16G"). If NULL, auto-detected.}

\item{resample_df}{External resampling pool. If NULL, \code{df} is used.}

\item{memory_save}{Logical flag for memory-efficient normalisation.}

\item{verbose}{Should the function print progress messages and logs?}

\item{cache_dir}{Character. Directory for on-disk caching of the internal
model fit (when \code{model} is NULL) and of every per-component
\code{\link{nm_normalise}} call in the decomposition loop -- see
\code{\link{nm_normalise}}'s \code{cache_dir} for why this matters
(each step is a full Monte Carlo resample-and-predict). If NULL
(default), caching is disabled.}

\item{variable_order}{Character vector or NULL. Explicit meteorological-
feature decomposition order, forwarded to \code{\link{nm_decom_met}}
(ignored when \code{method = "emission"}, which always uses its own
hardcoded calendar order). See \code{\link{nm_decom_met}}'s
\code{variable_order} for details.}
}
\value{
A data frame with the decomposed components.
}
\description{
\code{nm_decompose} is a high-level wrapper that performs time series decomposition,
separating a target variable (e.g., pollutant concentration) into components
driven by emissions/trends and meteorology.
}
\details{
This function supports two decomposition methods:
\itemize{
\item \strong{\code{emission}}: Isolates the influence of time-based features (Trend, Seasonality, Weekday, Hour).
The result is a breakdown of the "emissions-driven" or "human activity" signal.
\item \strong{\code{meteorology}}: Isolates the influence of individual meteorological features.
The result is a breakdown of the "meteorology-driven" signal.
}
If a pre-trained \code{model} is not provided, the function will first train one.
}
\examples{
\donttest{
if (requireNamespace("lightgbm", quietly = TRUE)) {
  covariates <- c("ws", "wd", "temp", "RH", "blh", "ssrd")
  predictors <- c(covariates, "date_unix", "day_julian", "weekday", "hour")
  build <- nm_build_model(
    my1[1:150, c("date", "NO2", covariates)],
    target = "NO2", covariates = predictors,
    model_config = list(n_trials = 1, cv_folds = 2, nrounds = 15,
                         num_leaves_min = 5, num_leaves_max = 15),
    seed = 42, verbose = FALSE
  )
  decomp <- nm_decompose(
    method = "emission", df = build$df_prep, model = build$model,
    covariates = predictors, n_samples = 2, n_cores = 1, verbose = FALSE
  )
  head(decomp)
}
}

}
