% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_pipeline_do_all.r
\name{nm_do_all}
\alias{nm_do_all}
\title{Perform All Steps for Meteorological Normalisation}
\usage{
nm_do_all(
  df = NULL,
  target = "value",
  backend = "lightgbm",
  covariates = NULL,
  resample_vars = NULL,
  resample_df = NULL,
  n_samples = 300,
  aggregate = TRUE,
  seed = 7654321,
  split_method = "random",
  train_fraction = 0.75,
  model_config = NULL,
  n_cores = NULL,
  max_mem_size = NULL,
  memory_save = TRUE,
  verbose = TRUE,
  cache_dir = NULL,
  ...
)
}
\arguments{
\item{df}{The raw input data frame.}

\item{target}{The target variable name as a string (default 'value').}

\item{backend}{The modeling backend to use for training: 'lightgbm' (default) or 'h2o'.}

\item{covariates}{The names of the features used for \strong{training}.}

\item{resample_vars}{Character vector of variables to resample (shuffle) during normalisation.
If NULL, defaults to all covariates except time variables.}

\item{resample_df}{External resampling pool. If NULL, \code{df} is used as the source pool.}

\item{n_samples}{Number of resampling iterations for normalisation.}

\item{aggregate}{Logical. If TRUE, returns aggregated means; if FALSE, returns raw rows.}

\item{seed}{A random seed for reproducibility.}

\item{split_method}{Method for splitting data for model training (e.g., 'random').}

\item{train_fraction}{Proportion of data used for training (default 0.75).}

\item{model_config}{A list of configuration parameters passed to the model training function.}

\item{n_cores}{Number of CPU cores to use for \strong{H2O cluster initialization}.}

\item{max_mem_size}{Maximum memory for H2O (e.g., "16G"). If NULL, auto-detected.}

\item{memory_save}{Logical. If TRUE, enables memory-efficient processing (batching/GC).}

\item{verbose}{Logical. Should the function print progress messages?}

\item{cache_dir}{Character. Directory for on-disk result caching. If NULL (default),
caching is disabled. When set, a hit on a matching cache key skips training
and normalisation entirely.}

\item{...}{Additional arguments passed to underlying functions (e.g. \code{output_dir}).}
}
\value{
A list containing:
\describe{
\item{res}{The normalised data frame.}
\item{model}{The trained model object.}
\item{df_prep}{The prepared data frame used for training.}
}
}
\description{
\code{nm_do_all} is a high-level convenience pipeline that:
\enumerate{
\item Initializes the computing cluster (H2O) with specific resources.
\item Prepares the data and trains a model (\code{nm_build_model}).
\item Runs the normalisation process (\code{nm_normalise}).
}
}
\examples{
\donttest{
if (requireNamespace("lightgbm", quietly = TRUE)) {
  covariates <- c("ws", "wd", "temp", "RH", "blh", "ssrd")
  predictors <- c(covariates, "date_unix", "day_julian", "weekday", "hour")
  result <- nm_do_all(
    my1[1:150, c("date", "NO2", covariates)],
    target = "NO2", covariates = predictors,
    model_config = list(n_trials = 1, cv_folds = 2, nrounds = 15,
                         num_leaves_min = 5, num_leaves_max = 15),
    n_samples = 5, seed = 42, n_cores = 1, memory_save = FALSE, verbose = FALSE
  )
  names(result)
  head(result$res)
}
}

}
