% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_io_gdas.r
\name{nm_fetch_gdas1}
\alias{nm_fetch_gdas1}
\title{Download GDAS1 weekly ARL files covering a date range}
\usage{
nm_fetch_gdas1(
  date_from,
  date_to,
  cache_dir,
  base_url = nm_arl_gdas1_base_url,
  overwrite = FALSE,
  on_missing = c("error", "warn"),
  timeout = 600
)
}
\arguments{
\item{date_from, date_to}{Inclusive range to cover; see
\code{\link{nm_gdas1_filenames}} (include the full backward window for
back-trajectories).}

\item{cache_dir}{Directory to download into / reuse from.}

\item{base_url}{Archive base URL. Default \code{nm_arl_gdas1_base_url}.}

\item{overwrite}{Logical; re-download even if the file exists. Default FALSE.}

\item{on_missing}{One of \code{"error"} or \code{"warn"} -- what to do if a
weekly file returns HTTP 404 (e.g. a date outside the archive coverage).}

\item{timeout}{Per-request timeout in seconds. Default 600.}
}
\value{
Character vector of local file paths, in chronological order.
}
\description{
Streams each weekly GDAS1 file (~570 MB) into \code{cache_dir} and returns
the local paths, ready to pass as \code{met_files} to
\code{\link{nm_run_back_trajectories}}. A file already present is reused.
}
