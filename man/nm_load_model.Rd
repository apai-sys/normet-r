% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_model_io.r
\name{nm_load_model}
\alias{nm_load_model}
\title{Load a saved model from a specified backend}
\usage{
nm_load_model(
  path = "./",
  filename = "automl",
  backend = "lightgbm",
  verbose = TRUE
)
}
\arguments{
\item{path}{A string specifying the directory path where the model is saved.}

\item{filename}{A string specifying the filename of the saved model.}

\item{backend}{A string specifying the model backend: 'lightgbm' (default) or 'h2o'.}

\item{verbose}{Should the function print log messages? Default is TRUE.}
}
\value{
The loaded model object.
}
\description{
\code{nm_load_model} is a high-level wrapper that dispatches the model loading task.
}
\examples{
\donttest{
if (requireNamespace("lightgbm", quietly = TRUE)) {
  covariates <- c("u10", "v10", "d2m", "t2m", "blh", "ssrd")
  predictors <- c(covariates, "date_unix", "day_julian", "weekday", "hour")
  build <- nm_build_model(
    my1[1:150, c("date", "NO2", covariates)],
    value = "NO2", predictors = predictors,
    model_config = list(n_trials = 1, cv_folds = 2, nrounds = 15,
                         num_leaves_min = 5, num_leaves_max = 15),
    seed = 42, verbose = FALSE
  )
  tmp <- tempfile()
  nm_save_model(build$model, path = tmp, filename = "model.rds", verbose = FALSE)
  model2 <- nm_load_model(path = tmp, filename = "model.rds", backend = "lightgbm", verbose = FALSE)
  unlink(tmp, recursive = TRUE)
}
}

}
