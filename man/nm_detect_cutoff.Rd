% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_analysis_events.r
\name{nm_detect_cutoff}
\alias{nm_detect_cutoff}
\title{Event-Based Placebo Detection}
\usage{
nm_detect_cutoff(series, method = "iqr", k = 3, seed = 7654321)
}
\arguments{
\item{series}{Numeric vector. If named (e.g. with \code{as.character(Date)}
names, as produced when subsetting a panel matrix column), the
names are used to recover \code{start} as a \code{Date} value;
otherwise an integer position is returned.}

\item{method}{Character. Anomaly method (\code{"iqr"}).}

\item{k}{Numeric. IQR threshold.}

\item{seed}{Integer. Random seed forwarded to \code{\link{nm_detect_events}}.}
}
\value{
A Date (or POSIXct) value of the earliest detected event start if
\code{series} carries date-like names, otherwise an integer
position; or \code{NULL} if no event is detected.
}
\description{
Automatically detect candidate cutoff dates from anomaly events.
Used internally by Bayesian SCM for automatic cutoff detection.
}
\keyword{internal}
