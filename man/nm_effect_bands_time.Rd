% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_causal_bands.r
\name{nm_effect_bands_time}
\alias{nm_effect_bands_time}
\title{Build Event-Time Uncertainty Bands from Placebo-in-Time Results}
\usage{
nm_effect_bands_time(
  placebo_time_out,
  level = 0.95,
  method = "quantile",
  horizon = NULL,
  return_segments = FALSE
)
}
\arguments{
\item{placebo_time_out}{List returned by \code{nm_placebo_in_time}.}

\item{level}{Numeric; confidence level for the bands (default 0.95).}

\item{method}{Character; \code{"quantile"} (default) or \code{"std"}.}

\item{horizon}{Integer or NULL; event-time window length K. If NULL,
uses the minimum segment length across all placebos.}

\item{return_segments}{Logical; if TRUE, also return the aligned
placebo segments matrix (default FALSE).}
}
\value{
A list with:
\itemize{
\item \code{bands}: data.frame indexed by \code{event_time} (0..K-1).
For \code{"quantile"}: columns \code{lower, upper, p10, p90}.
For \code{"std"}: columns \code{mean, std, lower, upper}.
\item \code{segments}: data.frame (K x P) or NULL.
\item \code{cutoffs}: character vector of placebo cutoff labels.
}
}
\description{
Constructs uncertainty bands aligned to event time (k = 0, 1, ..., K-1)
from the output of \code{nm_placebo_in_time}. Each placebo segment is
aligned to its own cutoff and truncated to a common horizon K.
}
