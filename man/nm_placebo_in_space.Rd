% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_causal_placebo.r
\name{nm_placebo_in_space}
\alias{nm_placebo_in_space}
\title{Placebo-in-Space Test for Synthetic Control}
\usage{
nm_placebo_in_space(
  df,
  date_col,
  unit_col,
  outcome_col,
  treated_unit,
  cutoff_date,
  donors = NULL,
  scm_backend = "scm",
  model_config = NULL,
  post_agg = "mean",
  n_cores = NULL,
  max_mem_size = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{df}{A long-format panel data frame.}

\item{date_col}{Name of the date column.}

\item{unit_col}{Name of the unit identifier column.}

\item{outcome_col}{Name of the outcome variable column.}

\item{treated_unit}{The identifier for the treated unit.}

\item{cutoff_date}{The intervention cutoff date (string or Date).}

\item{donors}{Optional vector of donor units. If NULL, uses all non-treated units.}

\item{scm_backend}{The backend to use: \code{"scm"} or \code{"mlscm"}.}

\item{model_config}{List of model parameters passed to the backend.}

\item{post_agg}{Method to aggregate post-treatment effects: \code{"mean"} or \code{"sum"}.}

\item{n_cores}{Number of CPU cores. For \code{scm_backend = "scm"}, donor
placebo runs are dispatched in parallel via \pkg{foreach}/\pkg{doSNOW}
using this many workers (default: detected cores minus one). For
\code{"mlscm"}, this instead sets the H2O cluster size and donors
are run sequentially (an H2O cluster isn't safely shared across
separate worker processes).}

\item{max_mem_size}{Maximum memory for H2O (e.g., "16G").}

\item{verbose}{Logical; whether to print log messages.}

\item{...}{Additional arguments passed to the internal SCM runner.}
}
\value{
A list containing:
\describe{
\item{treated}{Data frame of the effect for the actual treated unit.}
\item{placebos}{List of data frames for each placebo run.}
\item{p_value}{The calculated two-sided p-value.}
\item{ref_band}{Data frame containing aggregated quantiles (confidence bands) of the placebos.}
}
}
\description{
Runs placebo-in-space tests by iteratively treating each donor unit as if it
were the treated unit. This is used to calculate p-values for causal inference
by comparing the observed effect to the distribution of placebo effects.
}
\details{
\strong{Resource Management:}
If using \code{mlscm} (H2O), the cluster is initialized \strong{once} globally at the start.
The internal SCM runs attach to this existing cluster (by passing \code{n_cores=NULL}),
ensuring the connection remains active throughout the loop. The cluster is
shut down automatically when the function exits.
}
