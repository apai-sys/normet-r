% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_placebo_in_time.r
\name{nm_placebo_in_time}
\alias{nm_placebo_in_time}
\title{Placebo-in-Time Test}
\usage{
nm_placebo_in_time(
  df,
  date_col = "date",
  unit_col = "code",
  outcome_col = "poll",
  treated_unit = NULL,
  cutoff_date = NULL,
  donors = NULL,
  scm_backend = "scm",
  post_agg = "mean",
  min_pre_period = 30,
  placebo_every = 7,
  model_config = NULL,
  n_cores = NULL,
  max_mem_size = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{df}{A long-format panel data frame.}

\item{date_col}{Name of the date column.}

\item{unit_col}{Name of the unit identifier column.}

\item{outcome_col}{Name of the outcome variable column.}

\item{treated_unit}{The identifier for the treated unit.}

\item{cutoff_date}{The intervention cutoff date (string or Date).}

\item{donors}{Optional vector of donor units. If NULL, uses all non-treated units.}

\item{scm_backend}{The backend to use: \code{"scm"} or \code{"mlscm"}.}

\item{post_agg}{Method to aggregate post-treatment effects: \code{"mean"} or \code{"sum"}.}

\item{min_pre_period}{Integer. Minimum pre-period days before placebo
cutoff. Default 30.}

\item{placebo_every}{Integer. Step size (in days) between placebo cutoffs.
Default 7.}

\item{model_config}{List of model parameters passed to the backend.}

\item{n_cores}{Number of CPU cores. For \code{scm_backend = "scm"}, the
placebo cutoffs are run in parallel via \pkg{foreach}/\pkg{doSNOW}
using this many workers (default: detected cores minus one). For
\code{"mlscm"}, this instead sets the H2O cluster size and the
placebo cutoffs are run sequentially.}

\item{max_mem_size}{Maximum memory for H2O (e.g., "16G").}

\item{verbose}{Logical; whether to print log messages.}

\item{...}{Additional arguments passed to the internal SCM runner.}
}
\value{
A list with \code{treated} (data.frame), \code{placebos} (list of
data.frames), \code{p_value}, \code{ref_band_event_time}
(data.frame of quantiles by event time \code{k}), and
\code{placebo_stats} (named numeric).
}
\description{
Runs placebo-in-time tests by shifting the treatment cutoff date to
placebo positions before the true cutoff. The distribution of placebo
effects provides a p-value for the true estimate.
}
