% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_pipeline_do_all.r
\name{nm_do_all_unc}
\alias{nm_do_all_unc}
\title{Perform Normalisation with Uncertainty Estimation (Ensemble)}
\usage{
nm_do_all_unc(
  df = NULL,
  value = "value",
  backend = "lightgbm",
  predictors = NULL,
  resample_vars = NULL,
  resample_df = NULL,
  n_samples = 300,
  n_models = 5,
  memory_save = TRUE,
  confidence_level = 0.95,
  weighted_method = "r2",
  seed = 7654321,
  split_method = "random",
  fraction = 0.75,
  model_config = NULL,
  n_cores = NULL,
  max_mem_size = NULL,
  verbose = TRUE,
  cleanup_every = 5
)
}
\arguments{
\item{df}{The raw input data frame.}

\item{value}{The target variable name as a string (default 'value').}

\item{backend}{The modeling backend to use (default 'lightgbm', or 'h2o').}

\item{predictors}{Character vector of features used for \strong{training}.}

\item{resample_vars}{Character vector of variables to resample during normalisation.}

\item{resample_df}{External resampling pool.}

\item{n_samples}{Number of resampling iterations per model for normalisation.}

\item{n_models}{Number of models to train for the ensemble (default 5).}

\item{memory_save}{Logical. If TRUE, enables aggressive memory management.}

\item{confidence_level}{The confidence level for uncertainty bands (default 0.95).}

\item{weighted_method}{Metric for model weighting: "r2" (default) or "rmse".}

\item{seed}{Base random seed for reproducibility.}

\item{split_method}{Method for splitting data (e.g., 'random').}

\item{fraction}{Proportion of data used for training (default 0.75).}

\item{model_config}{List of configuration parameters for \code{nm_build_model}.}

\item{n_cores}{Number of CPU cores to use for \strong{H2O Initialization}.}

\item{max_mem_size}{Maximum memory for H2O (e.g., "16G"). If NULL, auto-detected.}

\item{verbose}{Logical. Print progress?}

\item{cleanup_every}{Integer. How often (in iterations) to clear H2O memory.}
}
\value{
A list containing:
\describe{
\item{res}{A data frame with the \code{observed}, \code{mean} normalised prediction, \code{weighted} prediction, and uncertainty bands (\code{lower_bound}, \code{upper_bound}).}
\item{mod_stats}{A summary table of performance metrics (R2, RMSE) for all trained models.}
}
}
\description{
\code{nm_do_all_unc} runs a robust uncertainty estimation pipeline by training multiple models
(Ensemble Learning) with different random seeds. It aggregates their predictions to provide
confidence intervals and a performance-weighted consensus prediction.
}
\details{
The function performs the following steps:
\enumerate{
\item \strong{Ensemble Training}: Trains \code{n_models} independent models using different random seeds.
\item \strong{Normalisation}: Each model produces its own weather-normalised trend.
\item \strong{Aggregation}: Calculates the Mean, Median, Standard Deviation, and Confidence Intervals (e.g., 95\%) across all models.
\item \strong{Weighted Consensus}: Calculates a weighted average trend based on model performance (R2 or RMSE) on the test set.
}
}
