% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_analysis_events.r
\name{nm_anomaly_scores}
\alias{nm_anomaly_scores}
\title{Compute Anomaly Scores for a Time Series}
\usage{
nm_anomaly_scores(
  series,
  value_col = NULL,
  method = "iqr",
  stl_period = NULL,
  seed = 7654321
)
}
\arguments{
\item{series}{Numeric vector or data frame with a \code{value} column.}

\item{value_col}{Character. Column name if \code{series} is a data frame.}

\item{method}{Character. One of \code{"iqr"}, \code{"isolation"}, or
\code{"stl_residual"}.}

\item{stl_period}{Integer. Period for STL decomposition (e.g., 24 for hourly
data with diurnal cycle). Only used when \code{method = "stl_residual"}.
If NULL, defaults to 24.}

\item{seed}{Integer. Random seed for reproducible methods (currently used by
\code{method = "isolation"}). Default \code{7654321}.}
}
\value{
A numeric vector of anomaly scores (same length as input).
}
\description{
Compute per-timestamp anomaly scores using one of three methods.
}
