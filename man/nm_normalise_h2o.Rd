% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_norm_normalise.r
\name{nm_normalise_h2o}
\alias{nm_normalise_h2o}
\title{Normalise Data using an H2O Model (Auto-Feature Detection)}
\usage{
nm_normalise_h2o(
  df,
  model,
  resample_vars = NULL,
  n_samples = 300,
  samples_per_batch = NULL,
  replace = TRUE,
  aggregate = TRUE,
  seed = 7654321,
  resample_df = NULL,
  memory_save = TRUE,
  verbose = TRUE,
  output_dir = NULL,
  file_format = "parquet",
  n_cores = NULL,
  cl = NULL
)
}
\arguments{
\item{df}{The input data frame or data.table. Must contain a 'date' column.}

\item{model}{The trained H2O model object (class \code{H2OModel}).}

\item{resample_vars}{A character vector of variables to be resampled.
If NULL, defaults to all model features excluding time components.}

\item{n_samples}{Integer. Total number of resampling iterations (e.g., 300 or 1000).}

\item{samples_per_batch}{Integer or NULL. Auto-calculated if NULL.}

\item{replace}{Logical. Whether to sample weather conditions with replacement. Default is TRUE.}

\item{aggregate}{Logical. TRUE returns mean per date; FALSE returns all simulations.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{resample_df}{External data frame to sample weather from. If NULL, \code{df} is used.}

\item{memory_save}{Logical. If TRUE, enables aggressive GC and strict batching.}

\item{verbose}{Logical. If TRUE, prints detailed logs.}

\item{output_dir}{Character (Optional). Directory for disk offloading.}

\item{file_format}{Character. "parquet", "csv", or "rds". Default "parquet".}

\item{n_cores}{Integer or NULL. If provided, overrides the conservative
default of 2 R-side resampling workers (still capped at
\code{detectCores() - 1}). Ignored if \code{cl} is supplied.}

\item{cl}{Optional existing parallel cluster object.}
}
\value{
A data frame (if aggregated or small raw) or file paths (if disk offloading).
}
\description{
\code{nm_normalise_h2o} performs meteorological normalisation (deweathering) using a trained H2O model.

\strong{Key Features:}
\enumerate{
\item \strong{Auto-Feature Detection}: Automatically extracts predictor names from the H2O model.
\item \strong{Resource Optimized}: Automatically restricts R side parallelism to 2 cores to reserve CPU/RAM for H2O.
\item \strong{Memory Safety}: Uses "Auto-Batching" to chunk data and prevent H2O timeouts.
\item \strong{Smart Disk Offloading}: Automatically switches to disk (Parquet) if output > 50M rows.
}
}
