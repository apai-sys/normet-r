% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_norm_decom.r
\name{nm_decom_emi}
\alias{nm_decom_emi}
\title{Decompose Emissions Influences (Trend, Seasonality, Weather)}
\usage{
nm_decom_emi(
  df = NULL,
  model = NULL,
  value = "value",
  predictors = NULL,
  backend = "lightgbm",
  split_method = "random",
  fraction = 0.75,
  model_config = NULL,
  n_samples = 300,
  seed = 7654321,
  n_cores = NULL,
  max_mem_size = NULL,
  resample_df = NULL,
  memory_save = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{df}{The input data frame. Must contain a 'date' column and the target variable.}

\item{model}{Pre-trained model. If NULL, a new model will be trained.}

\item{value}{The target variable name as a string (default 'value').}

\item{predictors}{Character vector of features used for \strong{training} (if model is NULL).}

\item{backend}{The modeling backend to use (default 'lightgbm', or 'h2o').}

\item{split_method}{Method for splitting data (e.g., 'random').}

\item{fraction}{Proportion of data used for training (default 0.75).}

\item{model_config}{List of configuration parameters for model training.}

\item{n_samples}{Number of resampling iterations per step (default 300).}

\item{seed}{Random seed for reproducibility.}

\item{n_cores}{Number of CPU cores to use for \strong{H2O Initialization} (when
\code{backend = "h2o"}) and for parallel resampling in each
\code{nm_normalise()} call (default: detected cores minus one).}

\item{max_mem_size}{Maximum memory for H2O (e.g., "16G"). If NULL, auto-detected.}

\item{resample_df}{External resampling pool. \strong{Important}: Usually the full dataset.
If NULL, defaults to \code{df}.}

\item{memory_save}{Logical. Enable memory-efficient processing.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
A data frame containing:
\itemize{
\item \code{date}: Timestamp.
\item \code{observed}: Original value.
\item \code{emi_total}: The fully normalised value (Time Fixed, Weather Shuffled).
\item \code{emi_base}: The global constant baseline.
\item \code{emi_noise}: Residual noise (\code{base} - \code{emi_base}).
\item Component columns: \code{date_unix} (Trend), \code{day_julian} (Seasonality), \code{weekday}, \code{hour} (if applicable).
}
}
\description{
\code{nm_decom_emi} performs a "Freeze-and-Shuffle" decomposition to isolate the contributions
of different time components (Trend, Seasonality, Weekday, Hour) from weather variability.
}
\details{
The function works by iteratively "freezing" time components while keeping others (and weather) shuffled:
\enumerate{
\item \strong{Base State}: Everything (Time + Weather) is shuffled. Result: Global Mean.
\item \strong{Trend State}: \code{date_unix} is frozen; others shuffled. Result: Trend line.
\item \strong{Seasonal State}: \code{date_unix} + \code{day_julian} frozen. Result: Trend + Seasonality.
\item \strong{...and so on}.
}
The final differences between these states reveal the net contribution of each component.
}
