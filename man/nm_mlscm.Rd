% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_causal_scm.r
\name{nm_mlscm}
\alias{nm_mlscm}
\title{Machine Learning Synthetic Control (ML-SCM)}
\usage{
nm_mlscm(
  df,
  date_col,
  unit_col,
  outcome_col,
  treated_unit,
  cutoff_date,
  donors,
  backend = "lightgbm",
  model_config = NULL,
  split_method = "random",
  fraction = 1,
  seed = 7654321,
  n_cores = NULL,
  max_mem_size = NULL,
  verbose = TRUE
)
}
\arguments{
\item{df}{Long-format panel data.}

\item{date_col}{Name of the date column.}

\item{unit_col}{Name of the unit identifier column.}

\item{outcome_col}{Name of the outcome variable.}

\item{treated_unit}{Name of the treated unit.}

\item{cutoff_date}{Date string or object marking the intervention point.}

\item{donors}{Character vector of donor units.}

\item{backend}{ML backend to use (default "lightgbm").}

\item{model_config}{List of backend-specific model configuration parameters (e.g., \code{nfold}, \code{algorithm}).}

\item{split_method}{Data splitting method for validation ("random", "time", etc.).}

\item{fraction}{Fraction of pre-treatment data used for training (default 1.0).}

\item{seed}{Random seed for reproducibility.}

\item{n_cores}{Number of CPU cores for H2O. If NULL, connects to an existing cluster or detects cores automatically.}

\item{max_mem_size}{Maximum memory for H2O (e.g., "16G").}

\item{verbose}{Whether to print progress messages.}
}
\value{
A data frame with columns: \code{date}, \code{observed}, \code{synthetic}, and \code{effect}.
}
\description{
\code{nm_mlscm} estimates counterfactual outcomes using a machine learning
backend. It pivots the panel to wide format (donors as features), trains a
model on the pre-treatment period, and extrapolates to estimate the
counterfactual.
}
\note{
\strong{Exploratory use only.} ML-SCM lacks the causal guarantees of
classic SCM (Abadie 2010). Key limitations: (1) ML models can overfit
the pre-treatment period when the number of donors approaches the
pre-period length, inflating post-treatment fit and biasing effect
estimates; (2) the RMSPE ratio placebo test is unreliable with ML-SCM
because near-perfect pre-period fit deflates the denominator; (3) no
interpretable donor weights are produced. For inferential conclusions
prefer \code{nm_scm} (lasso), \code{nm_scm_abadie} (QP), \code{nm_scm_mcnnm},
or \code{nm_bayesian_scm}.
}
