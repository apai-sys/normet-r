% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_io_trajectory.r
\name{nm_build_trajectory_features}
\alias{nm_build_trajectory_features}
\title{Build a receptor-time feature table from HYSPLIT tdump files}
\usage{
nm_build_trajectory_features(
  tdumps,
  source_regions = NULL,
  prefix = "traj_",
  date_col = "date"
)
}
\arguments{
\item{tdumps}{Character. A glob pattern (e.g. \code{"traj/tdump_*"}) or a vector
of \code{tdump} file paths. One back-trajectory run per receptor time is the
typical layout; files holding several trajectories are split per \code{traj}.}

\item{source_regions, prefix}{Forwarded to \code{\link{nm_trajectory_features}}.}

\item{date_col}{Character. Name of the receptor-timestamp column in the
output, so it joins straight onto a date-keyed panel. Default \code{"date"}.}
}
\value{
A data.frame with a \code{date_col} column (receptor timestamp) and one
\verb{<prefix>*} column per feature, sorted by time and deduplicated on the
receptor timestamp (last wins).
}
\description{
Reduce many HYSPLIT \code{tdump} files to a transport-feature table, one row per
receptor timestamp, ready to merge onto a date-keyed air-quality panel.
}
