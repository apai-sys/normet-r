% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_model_pdp.r
\name{nm_pdp}
\alias{nm_pdp}
\title{Compute Partial Dependence Plots (PDP)}
\usage{
nm_pdp(df, model, var_list = NULL, verbose = TRUE, ...)
}
\arguments{
\item{df}{Data frame containing the input data.}

\item{model}{The trained model object. Must have a 'backend' attribute.}

\item{var_list}{Character vector of variables to compute PDP for. If NULL, computes for all features.}

\item{verbose}{Logical. If TRUE, shows progress bars and logs.}

\item{...}{Additional arguments passed to the backend functions.}
}
\value{
A data frame with PDP results.
}
\description{
Main dispatcher function that routes PDP calculation to either the H2O backend
or the Generic backend based on the model type.
}
\examples{
\donttest{
if (requireNamespace("lightgbm", quietly = TRUE)) {
  covariates <- c("u10", "v10", "d2m", "t2m", "blh", "ssrd")
  predictors <- c(covariates, "date_unix", "day_julian", "weekday", "hour")
  build <- nm_build_model(
    my1[1:150, c("date", "NO2", covariates)],
    value = "NO2", predictors = predictors,
    model_config = list(n_trials = 1, cv_folds = 2, nrounds = 15,
                         num_leaves_min = 5, num_leaves_max = 15),
    seed = 42, verbose = FALSE
  )
  pdp <- nm_pdp(build$df_prep, build$model, var_list = c("t2m"), n_cores = 1, verbose = FALSE)
  head(pdp)
}
}

}
