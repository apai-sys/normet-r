% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_io_trajectory.r
\name{nm_run_back_trajectories}
\alias{nm_run_back_trajectories}
\title{Run HYSPLIT back-trajectories and reduce them to features}
\usage{
nm_run_back_trajectories(
  times,
  lat,
  lon,
  met_files,
  hysplit_exec,
  height_m = 500,
  hours_back = 72,
  work_dir = NULL,
  top_of_model = 10000,
  vert_motion = 0,
  diagnostics = .ALL_DIAGNOSTICS,
  source_regions = NULL,
  prefix = "traj_",
  timeout = 600
)
}
\arguments{
\item{times}{Vector of receptor (arrival) timestamps (POSIXct or anything
\code{as.POSIXct} accepts).}

\item{lat, lon}{Numeric receptor location (degrees).}

\item{met_files}{Character vector of ARL met file(s). They must collectively
cover the whole backward window (\code{hours_back} before each receptor time),
else the trajectory truncates where the data runs out.}

\item{hysplit_exec}{Path to the \code{hyts_std} executable
(e.g. \code{"~/hysplit-5.4.2/exec/hyts_std"}).}

\item{height_m}{Numeric receptor start height (m AGL). Default 500.}

\item{hours_back}{Integer backward duration in hours (sign ignored). Default 72.}

\item{work_dir}{Directory for \code{CONTROL}/\verb{tdump_*}. A temp dir is created if
\code{NULL}; tdumps are left there for inspection/reuse.}

\item{top_of_model, vert_motion}{HYSPLIT \code{CONTROL} settings (model top in m;
\code{0} = use met vertical velocity).}

\item{diagnostics}{Character vector of along-trajectory meteorological
variables \code{hyts_std} should write into \code{tdump} — subset of \code{"pressure"},
\code{"rainfall"}, \code{"blh"}, \code{"rh"}, \code{"temp"} — written into a \code{SETUP.CFG}
alongside \code{CONTROL}. Default is all of them. These feed
\code{\link{nm_trajectory_features}}'s optional \code{traj_rain_sum}/
\code{traj_blh_mean}/\code{traj_rh_mean}/\code{traj_pressure_mean}/\code{traj_temp_mean}
columns; pass fewer to skip the ones you don't need.}

\item{source_regions, prefix}{Forwarded to
\code{\link{nm_build_trajectory_features}}.}

\item{timeout}{Per-run timeout (seconds) for \code{hyts_std}. Default 600.}
}
\value{
The \code{\link{nm_build_trajectory_features}} table (one row per
receptor time).
}
\description{
For each receptor timestamp, write a HYSPLIT \code{CONTROL} file, run the
\code{hyts_std} executable (one backward trajectory ending at \verb{(lat, lon, height_m)}), then pass all resulting \code{tdump} files to
\code{\link{nm_build_trajectory_features}}.
}
\details{
You must supply a built \code{hyts_std} and ARL-format met files; this helper
only orchestrates the runs and parses the output.
}
\section{macOS note}{

HYSPLIT ships \code{x86_64} binaries (run under Rosetta on Apple Silicon)
downloaded via a disk image, so they carry a Gatekeeper quarantine flag. If
\code{hyts_std} is killed (exit 137) clear it once:
\preformatted{xattr -dr com.apple.quarantine /path/to/hysplit}
}

